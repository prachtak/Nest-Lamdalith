# Development environment variables (local dev)

# Core
STAGE=dev
REGION=eu-central-1
TABLE_NAME=games

# LocalStack endpoint (only for local development)
DYNAMO_ENDPOINT=http://localhost:4566

# CORS (dev can be permissive)
ALLOWED_ORIGINS=*

# Optional application version override
# APP_VERSION=0.1.0
